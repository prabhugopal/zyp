#!/usr/bin/env bash
# Friendly entry point for the orchestrator CLI — a thin wrapper, not a separate program.
# `./zypit run --scenario greenfield` is exactly `uv run python cli.py run --scenario greenfield`.
set -euo pipefail
cd "$(dirname "$0")"
exec uv run python cli.py "$@"
