# Request: CSV export for link analytics

The existing `GET /api/v1/links/{code}/analytics` endpoint returns aggregated click data as JSON.
Add a way to export the underlying recent-clicks data as a CSV file, so it can be opened directly
in a spreadsheet.
